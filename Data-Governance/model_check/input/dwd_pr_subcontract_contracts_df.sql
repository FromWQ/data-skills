CREATE TABLE IF NOT EXISTS `dwd_pr_subcontract_contracts_df`(
    `id` string COMMENT'主键ID',
    `create_user_code` string COMMENT'创建者账号',
    `create_time` timestamp COMMENT'创建时间',
    `update_user_code` string COMMENT'修改者账号',
    `update_time` timestamp COMMENT'修改时间',
    `tenant_id` bigint COMMENT'租户ID',
    `sync_es_flag` tinyint COMMENT'是否同步es(1:是，0：否)',
    `version` int COMMENT'乐观锁',
    `target_result_id` bigint COMMENT'定标结果Id',
    `target_result_name` string COMMENT'定标结果名称',
    `project_id` bigint COMMENT'项目Id',
    `project_name` string COMMENT'',
    `bill_code` string COMMENT'合同编码',
    `contract_name` string COMMENT'',
    `contract_category_id` bigint COMMENT'合同类别Id',
    `contract_category_name` string COMMENT'合同类别名称',
    `first_party_id` bigint COMMENT'甲方Id',
    `first_party_name` string COMMENT'甲方名称',
    `supplier_id` bigint COMMENT'供应商Id',
    `supplier_name` string COMMENT'供应商名称',
    `sign_date` date COMMENT'签订日期',
    `sign_place` string COMMENT'签约地点',
    `signature_status` string COMMENT'签章状态：1、未签章；2、待乙方签章；3、待甲方签章；4、已签章；  单据新增后默认为未签章',
    `performance_status` string COMMENT'合同履约状态：1、未签订；2、履约中；3、已封账；4、已解除；   默认为未签定，签章状态为已签章时，状态自动更改为【履约中】。当合同存在生效的最终结算时，状态自动改为【已封账】',
    `employee_id` bigint COMMENT'经办人Id',
    `employee_name` string COMMENT'经办人名称',
    `employee_phone` string COMMENT'甲方经办人电话',
    `supplier_employee` string COMMENT'乙方经办人',
    `supplier_employee_phone` string COMMENT'乙方经办人电话',
    `project_address` string COMMENT'工程地点',
    `tax_rate` decimal(28,8) COMMENT'合同税率',
    `quality_standard_id` string COMMENT'质量标准Id',
    `quality_standard_name` string COMMENT'质量标准名称',
    `project_tax_calc_type` string COMMENT'项目计税方式名称:简易计税、一般计税',
    `pricing_type_id` bigint COMMENT'计价方式Id',
    `pricing_type` string COMMENT'计价方式：1-可调单价、2-固定总价、3-成本加酬金；',
    `draft_type` string COMMENT'起草方式：1、在线起草，2、上传合同，3、线下签订',
    `contract_file_path` string COMMENT'合同附件路径',
    `contract_file_id` bigint COMMENT'合同附件Id',
    `watermark_contract_file_id` bigint COMMENT'水印合同附件Id',
    `contract_template_id` bigint COMMENT'合同模板Id',
    `contract_template_name` string COMMENT'合同模板名称',
    `contract_file_version_id` bigint COMMENT'合同附件版本Id',
    `contract_file_version` bigint COMMENT'合同附件版本',
    `qualify_id` bigint COMMENT'资质Id',
    `qualify_name` string COMMENT'资质名称',
    `qualify_certify_no` string COMMENT'资质证书编号',
    `qualify_licence_issuing_authority` string COMMENT'资质证书发证机关',
    `qualify_grant_date` date COMMENT'资质证书授予日期',
    `qualify_valid_till_date` date COMMENT'资质证书有效期至',
    `safety_production_license_no` string COMMENT'安全生成许可证编号',
    `safety_certify_grant_date` date COMMENT'安全生产许可证授予日期',
    `safety_certify_valid_till_date` date COMMENT'安全许可证有效期至',
    `project_start_date` date COMMENT'开工日期',
    `planned_finish_date` date COMMENT'计划竣工日期',
    `contract_days_limit` int COMMENT'合同工期:合同工期=计划竣工日期-开工日期+1（单位：天）',
    `first_party_project_manager_id` bigint COMMENT'甲方项目经理Id',
    `first_party_project_manager_name` string COMMENT'甲方项目经理名称',
    `first_party_project_manager_link` string COMMENT'甲方项目经理联系电话',
    `first_party_project_manager_post` string COMMENT'甲方项目经理职务',
    `supplier_project_manager_id` bigint COMMENT'乙方项目经理Id',
    `supplier_project_manager_name` string COMMENT'乙方项目经理名称',
    `supplier_project_manager_link` string COMMENT'乙方项目经理联系电话',
    `supplier_project_manager_post` string COMMENT'乙方项目经理职务',
    `supplier_project_manager_id_card` string COMMENT'乙方项目经理身份证号',
    `contract_tax_mny` decimal(28,8) COMMENT'合同金额（含税）',
    `contract_mny` decimal(28,8) COMMENT'合同金额（无税）',
    `contract_tax` decimal(28,8) COMMENT'合同税额',
    `sub_contract_tax_mny` decimal(28,8) COMMENT'分包金额（含税）',
    `sub_contract_mny` decimal(28,8) COMMENT'分包金额（无税）',
    `sub_contract_tax` decimal(28,8) COMMENT'分包税额',
    `other_cost_tax_mny` decimal(28,8) COMMENT'其他金额（含税）',
    `other_cost_mny` decimal(28,8) COMMENT'其他金额（无税）',
    `other_cost_tax` decimal(28,8) COMMENT'其他税额',
    `bill_state` tinyint COMMENT'单据状态',
    `commit_date` timestamp COMMENT'提交日期',
    `commit_user_name` string COMMENT'提交人名称',
    `commit_user_code` string COMMENT'提交人账号',
    `effective_date` timestamp COMMENT'生效日期',
    `org_id` bigint COMMENT'项目部Id',
    `org_name` string COMMENT'',
    `parent_org_id` bigint COMMENT'项目部上级组织Id',
    `parent_org_name` string COMMENT'',
    `parent_org_code` string COMMENT'项目部上级组织编码',
    `contract_file_highlight_path` string COMMENT'合同高亮附件路径',
    `contract_file_highlight_id` bigint COMMENT'合同高亮附件Id',
    `base_mny` decimal(28,8) COMMENT'初始版本金额',
    `base_tax_mny` decimal(28,8) COMMENT'初始版本(含税)金额',
    `before_change_mny` decimal(28,8) COMMENT'变更前金额',
    `before_change_tax_mny` decimal(28,8) COMMENT'变更前金额（含税）',
    `changing_mny` decimal(28,8) COMMENT'变更中金额',
    `changing_tax_mny` decimal(28,8) COMMENT'变更中金额（含税）',
    `change_code` string COMMENT'变更单号',
    `change_version` int COMMENT'变更版本',
    `change_id` bigint COMMENT'变更主键',
    `change_status` tinyint COMMENT'变更状态(1-未变更 2-变更中 3-已变更)',
    `supplement_flag` tinyint COMMENT'是否补充协议(1:是，0：否)',
    `main_contract_id` bigint COMMENT'主合同Id',
    `main_contract_code` string COMMENT'主合同编码',
    `main_contract_name` string COMMENT'主合同名称',
    `supplement_describe` string COMMENT'补充项描述',
    `main_contract_create_date` timestamp COMMENT'主合同创建日期（用于合同、补充协议排序）',
    `change_date` date COMMENT'变更日期',
    `change_contract_signature_status` tinyint COMMENT'变更合同签章状态',
    `change_draft_type` string COMMENT'变更起草方式：1-上传附件，2-线下签订',
    `change_file_id` bigint COMMENT'变更附件Id',
    `change_contract_name` string COMMENT'变更合同名称',
    `signed_file_id` bigint COMMENT'签章完成合同文件Id',
    `contract_type` tinyint COMMENT'合同类型：劳务合同-0，专业合同-1',
    `add_type` tinyint COMMENT'合同新增方式：参照定标结果新增-0，直接新增-1',
    `project_code` string COMMENT'项目编码',
    `org_code` string COMMENT'项目部编码',
    `bill_push_flag` string COMMENT'单据推送标识：unSucPush-未成功推送供方，sucPushed-已成功推送供方',
    `invoice_type_id` bigint COMMENT'发票类型Id',
    `invoice_type_name` string COMMENT'发票类型名称',
    `artificial_tax_mny` decimal(28,8) COMMENT'人工费金额（含税）',
    `artificial_mny` decimal(28,8) COMMENT'人工费金额（无税）',
    `artificial_tax` decimal(28,8) COMMENT'人工费税额',
    `contract_file_sync_flag` tinyint COMMENT'线上起草合同，审批中修改的合同是否已重新进行合同编辑,默认为已同步',
    `artificial_scale` decimal(28,8) COMMENT'人工费比例（人工费金额/合同金额*100%）',
    `filing_status` tinyint COMMENT'是否归档 1：是；0：否',
    `filing_ref` tinyint COMMENT'是否被归档引用 1：是；0：否',
    `contract_category_property` string COMMENT'合同分类属性',
    `main_scope_of_contract` string COMMENT'主要承包范围',
    `currency_id` bigint COMMENT'币种id',
    `guarantee_id` bigint COMMENT'担保形式id',
    `guarantee_proportion` decimal(28,8) COMMENT'担保比例',
    `guarantee_sun` decimal(28,8) COMMENT'担保金额',
    `validity_of_guarantee` date COMMENT'保函有效期',
    `method_id` bigint COMMENT'分包方式id',
    `guarantee_file_id` bigint COMMENT'保函附件id',
    `purchase_category` bigint COMMENT'采购类别',
    `contract_id` bigint COMMENT'承包合同',
    `notender_purchase` string COMMENT'非招标采购',
    `notender_purchase_id` string COMMENT'非招标采购id',
    `notender_purchase_code` string COMMENT'非招标Code编码',
    `bank_account_name` string COMMENT'开户银行名称',
    `bank_account_code` string COMMENT'银行账号',
    `inner_flag` tinyint COMMENT'是否内部合同(0 否 1 是)',
    `is_add` string COMMENT'旧系统新增',
    `old_id` string COMMENT'旧系统ID',
    `whether_round` tinyint COMMENT'是否取整0:不取整 1:取整',
    `bidding_code` string COMMENT'招标编号',
    `exchange_rate` decimal(28,8) COMMENT'汇率',
    `local_contract_tax_mny` decimal(28,8) COMMENT'合同本币金额（含税）',
    `local_contract_mny` decimal(28,8) COMMENT'合同本币金额（不含税）',
    `local_contract_tax` decimal(28,8) COMMENT'合同本币税额',
    `local_base_tax_mny` decimal(28,8) COMMENT'初始合同本币金额(含税)',
    `local_base_mny` decimal(28,8) COMMENT'初始合同本币金额（不含税）',
    `local_base_tax` decimal(28,8) COMMENT'初始合同本币税额'
 )comment'项目劳务分包合同'partitioned by(
    dt string
 )stored as orc 
 lifecycle 100;
