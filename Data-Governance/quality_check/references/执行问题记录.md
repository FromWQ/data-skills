# 数仓质量评估 Skill - 执行问题记录

## 执行时间：2026-07-06

---

## 一、执行结果

### 1.1 整体评分

| 指标 | 数值 |
|------|------|
| **整体评分** | 72.62 分（一般） |
| 规则总数 | 32 条 |
| 通过规则 | 25 条 |
| 规则通过率 | 78.1% |
| 阻断问题数 | 1 个 |
| 警告问题数 | 2 个 |
| 提示问题数 | 4 个 |

### 1.2 分层评分

| 分层 | 评分 | 等级 |
|------|------|------|
| ODS | 98.0 | 优秀 |
| DWD | 88.5 | 良好 |
| DWS | 100.0 | 优秀 |
| DIM | 0 | 无数据 |
| ADS | 72.0 | 一般 |

---

## 二、检测到的真实问题

### 2.1 阻断级问题 (BLOCK)

| 表名 | 规则 | 问题描述 |
|------|------|---------|
| ads_pr_management_condition_info_df | ADS_002 | 关键指标非空检查失败 |

**验证 SQL**：
```sql
SELECT COUNT(*) as total_count,
    SUM(CASE WHEN project_code IS NULL OR project_code = '' THEN 1 ELSE 0 END) as null_count
FROM ads_pr_management_condition_info_df
-- 结果：4 条空值
```

### 2.2 警告级问题 (WARN)

| 表名 | 规则 | 问题描述 |
|------|------|---------|
| dwd_pr_material_check_df | COM_004 | 时间字段合理性检查失败 |
| dwd_pr_settle_volumes_info_df | COM_004 | 时间字段合理性检查失败 |

**验证 SQL**：
```sql
SELECT COUNT(*) as total_count,
    SUM(CASE WHEN create_time < '1970-01-01' OR create_time > NOW() THEN 1 ELSE 0 END) as invalid_count
FROM dwd_pr_material_check_df
-- 结果：有异常时间数据
```

### 2.3 提示级问题 (INFO)

| 表名 | 规则 | 问题描述 |
|------|------|---------|
| ads_pr_management_condition_info_df | COM_005 | 脏数据/乱码检查 |
| dwd_pr_material_check_df | COM_005 | 脏数据/乱码检查 |
| dwd_pr_settle_volumes_info_df | COM_005 | 脏数据/乱码检查 |
| dws_ztpc_lj_receive_balance_month_df | COM_005 | 脏数据/乱码检查 |

---

## 三、数据验证

### 3.1 ads_pr_management_condition_info_df

```sql
-- NULL 值检查
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN business_year_month IS NULL THEN 1 ELSE 0 END) as null_month,
    SUM(CASE WHEN project_code IS NULL OR project_code = '' THEN 1 ELSE 0 END) as null_code
FROM ads_pr_management_condition_info_df;
-- 结果：100 条中有 2 条 NULL month, 4 条 NULL code ✅
```

### 3.2 ods_ztpc_xmglxt_ejc_prosub_settle_df

```sql
-- dr 字段检查
SELECT dr, COUNT(*) as cnt 
FROM ods_ztpc_xmglxt_ejc_prosub_settle_df 
GROUP BY dr;
-- 结果：dr 有 -1 和 99 的异常值（正常应该是 0/1）✅
```

### 3.3 dwd_pr_material_check_df

```sql
-- NULL 值检查
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN check_state IS NULL THEN 1 ELSE 0 END) as null_state
FROM dwd_pr_material_check_df;
-- 结果：98 条中有 5 条 NULL check_state ✅
```

---

## 四、已修复的问题

| 序号 | 问题 | 解决方案 | 状态 |
|------|------|---------|------|
| 1 | Python 语法错误 | 修复重复代码 | ✅ |
| 2 | 缺少 pymysql 依赖 | pip3 install pymysql | ✅ |
| 3 | 数据库地址格式错误 | 移除 jdbc:前缀 | ✅ |
| 4 | meta_table_column 缺少字段 | ALTER TABLE 添加 is_pk/is_partition/created_at/updated_at | ✅ |
| 5 | 规则文件路径错误 | 修改为正确路径 | ✅ |
| 6 | 规则解析正则不匹配 | 简化解析逻辑 | ✅ |
| 7 | SQL 占位符未替换 | 添加动态字段替换逻辑 | ✅ |
| 8 | COM_004/COM_005 字段不存在 | 使用_find_time_field 动态查找 | ✅ |

---

## 五、待优化问题

### 5.1 ODS_003 完全重复检查

**问题**：部分表没有 id 字段，导致 SQL 执行失败

**解决方案**：
1. 检查表是否有 id 字段，没有则跳过
2. 或者使用主键字段代替

### 5.2 报告内容不够详细

**问题**：问题明细部分缺少具体的异常数据示例

**解决方案**：
在 analysis_result 中添加异常样本数据

### 5.3 DIM 层无数据

**问题**：数据库中没有 dim_开头的表

**解决方案**：
确认是否有维度表，或者修改表名前缀识别逻辑

---

## 六、下次执行检查清单

- [x] 数据库连接正常
- [x] meta_table_column 表结构完整
- [x] rule_v2.md 文件存在
- [ ] 确认是否有 dim_开头的表
- [ ] 确认 ODS_003 规则是否需要适配无 id 字段的表

---

## 七、报告位置

**评估报告**：`Data-Governance/quality_check/output/quality_check_report.md`

---

*记录时间：2026-07-06 10:24*
*最后更新：2026-07-06 10:24*
